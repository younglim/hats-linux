---

   - name: Install npm
     become: yes
     apt:
      pkg:
        - npm
      state: present

   - name: Install appium
     become: yes
     npm:
       name: appium
       global: yes
       unsafe_perm: yes
       state: latest
